function getURL() {
    alert("The URL of this page is: " + window.location.href);
    const currentURL = window.location.href;
    console.log((currentURL));                  // This print url in console of the browser
}
